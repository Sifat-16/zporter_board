import 'package:flutter/material.dart';

class PlayersToolbarOther extends StatelessWidget {
  const PlayersToolbarOther({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
