import 'package:flutter/cupertino.dart';

class PlayersToolbarAway extends StatelessWidget {
  const PlayersToolbarAway({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
